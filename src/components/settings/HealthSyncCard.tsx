'use client'

/**
 * Health sync card disabled until native HealthKit / Health Connect ships.
 * Hidden for App Store review — no placeholder "connected" UX.
 */
export default function HealthSyncCard() {
  return null
}
