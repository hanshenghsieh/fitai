'use client'

/**
 * Apple Health UI disabled until HealthKit is fully wired.
 * Keep file so a future integration can re-enable without rediscovering layouts.
 * Do not mount this on SettingsScreen for App Store review builds.
 */
export default function SettingsHealthSection() {
  return null
}
